function five(string) {
	let library = ['pro', 'gram', 'merit', 'program', 'it', 'programmer'];
	let result = [];
	for (let i = 0; i <= string.length; i++) {
		result++
	}
	console.dir(result);
}

five('programit');
five('programmerit');